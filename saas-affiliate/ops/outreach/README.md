# 問い合わせ下書きの使い方

- 送るのは人。Claudeは下書きまで。
- **送るタイミングはサイト公開後**（記事1本でもURLがある状態）。URLのないメールは返信率が落ちる。
- `{SITE_URL}` `{YOUR_NAME}` を埋めてから送る。
- 返信が来ない場合は1回だけ追いかけて、それ以上は追わない（週2時間枠を守る）。
- 返ってきた数字は `../tracker.csv` に入れて `status` を `verified` にする。
