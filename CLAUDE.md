# CLAUDE.md

## 画像生成のルール（最優先・全作業共通）

**画像はAI側で生成しない。ChatGPT（GPT Image 2.0）用のプロンプトを出すところまでが担当。**

ちほはChatGPTに課金している。クレジット課金のMCPツールを使うと二重払いになるため使わない。

### 守ること

1. **クレジットを消費する画像生成ツールを勝手に叩かない。**
   対象：Higgsfield（`generate_image` / `gpt_image_2` 含む）、Adobe Firefly、Canva の生成系など。
   ちほが「これで作って」と明示的に指定したときだけ使う。
   ※ Higgsfield 経由の `gpt_image_2` は中身は同じ GPT Image 2 でも、課金は Higgsfield クレジット。ChatGPT の課金とは別勘定。

2. **画像が必要になったら、ChatGPTにそのまま貼れるプロンプトを出す。**
   - コピペしやすいようコードブロックで出す
   - サイズ・アスペクト比・テイスト・用途を書き込む
   - 画像内に文字を入れる場合は、入れる文字を「」で literal 指定する
   - 生成後の画像を渡されたら、その先の作業（分割・背景透過・リサイズ・HTMLへの組み込み等）はこちらでやる

3. **作図で済むものはコードで書く。**
   図解・バナー・アイコン・グラフ・装飾など「絵でなくても成立するもの」は SVG / HTML で直接書く。
   クレジットもChatGPTの生成回数も消費せず、後から文字だけ差し替えできる。
   そちらで足りそうなときは「SVGで書きますか？」と先に提案する。
